{
  "name": "inklude",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "convex": "convex"
  },
  "dependencies": {
    "convex": "^1.31.7",
    "@google/generative-ai": "^0.21.0"
  }
}